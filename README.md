# TAMS on kubernetes
This repository provides a kubernetes deployment of TAMS (Time Addressable Media Store).

## Getting started

You can deploy the TAMS controller using the following command:
```bash
kubectl apply -f https://raw.githubusercontent.com/trackit/tams-k8s/refs/heads/master/k8s-controller/artifacts/tams/quick-deploy.yaml
```